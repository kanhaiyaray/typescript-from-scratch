const storedValue: unknown = "TypeScript compiler";

const message2 = <string>storedValue;

console.log("Original value:", storedValue);
console.log("Uppercase value:", message.toUpperCase());

const characterCount = message.length;

console.log("Character count:", characterCount);
